using System;

public static class cmd
{
	//the flex command handler//
    //command handler handles commands and errors, calling on the proper script and executing it when needed//
    public static void thiscmd()
    {
        flog.i();
    }
}
